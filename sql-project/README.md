# SQL Database Build & Queries

**Tools:** SQL  
**Skills:** DDL, DML, joins, views, indexing, sequences, synonyms

## 📘 Overview
This project demonstrates my ability to work with SQL beyond basic SELECT statements. The script includes table creation, inserts, structural changes, and performance-related objects such as indexes and sequences.

## 🔧 What I Did
- Created multiple tables using `CREATE TABLE`  
- Inserted sample data using `INSERT INTO`  
- Modified structures using `ALTER TABLE`  
- Built a `VIEW` to simplify reporting  
- Created `SEQUENCE` objects for auto-incrementing values  
- Added `INDEX` objects to improve query performance  
- Created a `SYNONYM` for simplified table access  

## 📂 Files Included
- `database_build.sql` – full SQL script with all commands

